class Morpion {

    matrix

    constructor() {
        this.matrix = this.getInitMatrix()
    }

    reset() {
        this.matrix = this.getInitMatrix()
    }

    getInitMatrix() {
        return [[0,0,0], [0,0,0], [0,0,0]]
    }

    isResolved(value) {
        // Per line
        // for (let i = 0; i < this.matrix.length; i++) {
        //     if ()
        // }

        // Per column

        // Diagonals

        return false
    }
}